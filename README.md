# AI Email Triage OpenEnv

This project implements an OpenEnv-compliant environment for simulating email triage tasks.

## Features
- step(), reset(), state()
- Reward system (0–1 scoring)
- Multiple difficulty levels
- Baseline AI agent
- Streamlit UI

## Run
pip install streamlit pydantic  
streamlit run app/app.py
